import React from 'react'
import { Tabs } from '../../types/types'

type TabsType = {
    tabs: Tabs[]
}
const Stock: React.FC<TabsType> = ({ tabs }) => {
    return (
        <div>

        </div>
    )
}